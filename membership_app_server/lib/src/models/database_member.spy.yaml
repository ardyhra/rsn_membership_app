class: DatabaseMember
table: database_member
fields:
  buktiPembayaran: String  # Sekarang hanya menyimpan satu URL sebagai string
  keterangan: String       # Menyimpan deskripsi/keterangan pembayaran
  pelangganId: int, relation(parent=member)  # Relasi ke tabel Member berdasarkan ID
